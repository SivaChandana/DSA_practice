class Solution {
    public int lengthOfLastWord(String s) {
        String[] words=s.split(" ");
        int index=words.length-1;
         String st=words[index];
         return st.length();
    }
}
